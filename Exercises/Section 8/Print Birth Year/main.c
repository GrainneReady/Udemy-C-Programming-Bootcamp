#include <stdio.h>
#include <stdlib.h>

int main()
{
    int currentYear = 2022;
    int age = 20;
    printf("The year you were born in was: %d - %d = %d", currentYear, age, currentYear - age);
    return 0;
}